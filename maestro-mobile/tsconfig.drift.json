{
  "//": "DEDICATED drift-guard gate (Lexicon owns; Sentinel enforces). Typechecks",
  "//2": "ONLY src/domain/__sync__/** + the maestro-server type sources it imports.",
  "//3": "This is the ONLY tsconfig that reaches into ../maestro-server. A server-",
  "//4": "side type error surfaces HERE (a drift failure), never in the app gate.",
  "//5": "Standalone (does NOT extend expo/tsconfig.base) so RN/Expo globals and the",
  "//6": "app's include set stay out — server CJS types compile type-only & isolated.",
  "compilerOptions": {
    "strict": true,
    "noEmit": true,
    "skipLibCheck": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "forceConsistentCasingInFileNames": true,
    "types": [],
    "lib": ["es2022"],
    "baseUrl": "."
  },
  "include": [
    "src/domain/__sync__/**/*.ts",
    "../maestro-server/src/types.ts",
    "../maestro-server/src/domain/events/DomainEvents.ts"
  ],
  "exclude": ["node_modules"]
}
