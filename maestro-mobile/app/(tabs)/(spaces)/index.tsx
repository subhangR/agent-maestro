// Spaces home. Mounts the Collab SpacesScreen (auth gate → sign-in / spaces list).
import { SpacesScreen } from '@/features/collab';

export default function SpacesHomeRoute(): React.JSX.Element {
  return <SpacesScreen />;
}
