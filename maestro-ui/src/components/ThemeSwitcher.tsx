/**
 * The multi-theme picker has been retired. The app now ships a single
 * graphite + amber "baton" identity (see styles-tokens.css). This component
 * is kept as a no-op so existing render sites don't break; it intentionally
 * renders nothing.
 */
export function ThemeSwitcher() {
  return null;
}
